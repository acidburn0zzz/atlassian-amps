<project>
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.atlassian.amps.it</groupId>
    <artifactId>maven-amps-plugin-validate-manifest-skip</artifactId>
    <version>testing</version>
    <name>TestValidateManifestSkip</name>

    <build>
        <plugins>
            <plugin>
                <groupId>${shitty.groupId}</groupId>
                <artifactId>${shitty.artifactId}</artifactId>
                <version>testing</version>
            </plugin>
        </plugins>
    </build>

</project>