<project>
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.atlassian.amps.it</groupId>
    <artifactId>maven-amps-pom-deploy</artifactId>
    <version>testing</version>
    <packaging>pom</packaging>
    <name>TestUnitTest parent</name>
    <distributionManagement>
        <snapshotRepository>
            <id>nexus-snapshots</id>
            <url>file://${basedir}/target/snapshots</url>
        </snapshotRepository>
        <repository>
            <id>nexus-staging</id>
            <url>file://${basedir}/target/releases</url>
        </repository>
    </distributionManagement>
    <modules>
        <module>plugin</module>
        <module>jar</module>
    </modules>
    <build>
        <plugins>
            <plugin>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <source>1.6</source>
                    <target>1.6</target>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.sonatype.plugins</groupId>
                <artifactId>nexus-staging-maven-plugin</artifactId>
                <version>1.6.2</version>
                <extensions>true</extensions>
                <configuration>
                    <skipStaging>true</skipStaging>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-deploy-plugin</artifactId>
                <version>2.8.2</version>
                <configuration>
                    <!-- if this repo is used, nexus is not used? -->
                    <altDeploymentRepository>file://${basedir}/target/releases-wrong</altDeploymentRepository>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <dependencies>
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.10</version>
            <scope>test</scope>
        </dependency>
    </dependencies>
</project>