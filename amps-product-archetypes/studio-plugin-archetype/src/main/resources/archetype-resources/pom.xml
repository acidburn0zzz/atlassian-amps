#set( $dollar = '$' )
<?xml version="1.0" encoding="UTF-8"?>

<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">

    <modelVersion>4.0.0</modelVersion>
    <groupId>\${groupId}</groupId>
    <artifactId>\${artifactId}</artifactId>
    <version>\${version}</version>

    <organization>
        <name>Example Company</name>
        <url>http://www.example.com/</url>
    </organization>

    <name>\${artifactId}</name>
    <description>This is the \${artifactId} plugin for Atlassian Studio.</description>
    <packaging>atlassian-plugin</packaging>

    <dependencies>
        <dependency>
            <groupId>com.atlassian.jira</groupId>
            <artifactId>jira-page-objects</artifactId>
            <version>4.4-rc2</version>
            <scope>compile</scope>
        </dependency>
    </dependencies>
    
    <build>
        <plugins>
            <plugin>
                <groupId>com.atlassian.maven.plugins</groupId>
                <artifactId>maven-amps-plugin</artifactId>
                <version>\${dollar}{amps.version}</version>
                <extensions>true</extensions>
                <configuration>
                    <testGroup>studioTestGroup</testGroup>
                    <instanceId>studio1</instanceId>
                    <products>
                        <product>
                            <id>studio</id>
                            <instanceId>studio1</instanceId>
                            <version>\${dollar}{studio.version}</version>
                            <dataVersion>\${dollar}{studio.data.version}</dataVersion>
                        </product>
                    </products>
                    <instructions>
                        <!--
                            It is critical to provide correct plugin key (matches with the key atlassian-plugin.xml)
                              otherwise Atlassian Plugins system won't treat bundle as transformless plugin
                         -->
                        <Atlassian-Plugin-Key>${atlassian.plugin.key}</Atlassian-Plugin-Key>

                        <!-- Add package to export here -->
                        <Export-Package>
                            ${package}.api,
                        </Export-Package>

                        <!-- Add pacakage import here -->
                        <Import-Package>
                            com.atlassian.sal.api;version="[2.7, 4.0)"
                        </Import-Package>

                        <!-- Ensure plugin is spring powered - see https://extranet.atlassian.com/x/xBS9hQ  -->
                        <Spring-Context>*</Spring-Context>
                    </instructions>

                    <testGroups>
                        <testGroup>
                            <id>studioTestGroup</id>
                            <productIds>
                                <productId>studio1</productId>
                            </productIds>
                            <includes>
                                <include>it/**/*Test.java</include>
                            </includes>
                        </testGroup>
                    </testGroups>
                </configuration>
            </plugin>
            <plugin>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <source>${jdkLevel}</source>
                    <target>${jdkLevel}</target>
                </configuration>
            </plugin>
        </plugins>
    </build>
    
    <properties>
        <studio.version>${studio.version}</studio.version>
        <studio.data.version>${studio.data.version}</studio.data.version>
        <amps.version>${project.version}</amps.version>
        <plugin.testrunner.version>${plugin.testrunner.version}</plugin.testrunner.version>
        <!-- This key is used to keep the consistency between the key in atlassian-plugin.xml and the key to generate bundle. -->
        <atlassian.plugin.key>\${project.groupId}.\${project.artifactId}</atlassian.plugin.key>
    </properties>

</project>
