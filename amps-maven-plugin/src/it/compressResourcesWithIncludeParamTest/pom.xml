<project>
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.atlassian.amps.it</groupId>
    <artifactId>amps-maven-plugin-compress-resources-test</artifactId>
    <version>testing</version>
    <name>TestCompressResourcesWithIncludeParam</name>

    <build>
        <plugins>
            <plugin>
                <groupId>${invoker.groupId}</groupId>
                <artifactId>${invoker.artifactId}</artifactId>
                <version>${project.version}</version>
            </plugin>
        </plugins>
        <resources>
            <resource>
                <directory>src/main/resources</directory>
                <includes>
                    <include>js_.js</include>
                    <include>test-compressed.xml</include>
                    <include>foo.css.js-ext.jsp</include>
                </includes>
                <excludes>
                    <exclude>foo.css</exclude>
                </excludes>
            </resource>
            <resource>
                <directory>src/main/resources/css</directory>
                <includes>
                    <include>**/*</include>
                </includes>
            </resource>
        </resources>
    </build>
</project>