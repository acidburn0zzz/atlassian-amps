name: ${microsName}
description: ${microsDescription}
organization: ${microsOrganization}
links:
  binary:
    type: maven
    artifactId: ${artifactId}
    version: ${version}
    classifier: assembly
  healthcheck:
    uri: healthcheck
  source:
    url: '${microsSourceUrl}'
notifications:
  email: ${microsNotificationEmail}
owners:
  - ${microsOwnerEmail}